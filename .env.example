# Optional configuration (copy to .env)

# FinBERT model on HuggingFace
FINBERT_MODEL=ProsusAI/finbert

# Sentiment batch size (lower if low on RAM)
SENTIMENT_BATCH_SIZE=8

# Network retry settings
MAX_RETRIES=3
RETRY_DELAY_SECONDS=1.5
